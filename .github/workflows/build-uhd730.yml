name: Build UHD730 VirtIO Driver

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-2022

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install WDK build tools
        shell: powershell
        run: |
          $vsInstaller = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vs_installer.exe"

          & $vsInstaller modify `
            --installPath "C:\Program Files\Microsoft Visual Studio\2022\Enterprise" `
            --add Component.Microsoft.Windows.DriverKit.BuildTools `
            --add Microsoft.VisualStudio.Component.Windows11SDK.26100 `
            --quiet `
            --norestart

          if ($LASTEXITCODE -ne 0 -and $LASTEXITCODE -ne 3010) {
            exit $LASTEXITCODE
          }

      - name: Add MSBuild
        uses: microsoft/setup-msbuild@v2
        with:
          msbuild-architecture: x64

      - name: Build viogpu Win10 x64
        shell: cmd
        run: |
          call build\build.bat viogpu\viogpu.sln "Win10" amd64

      - name: Locate matching driver package
        shell: powershell
        run: |
          $sys = Get-ChildItem -Recurse -Filter viogpudo.sys |
                 Where-Object { $_.FullName -match 'amd64|x64' } |
                 Select-Object -First 1

          if (-not $sys) {
            throw "viogpudo.sys not found"
          }

          $dir = $sys.Directory.FullName

          Write-Host "Driver directory:"
          Write-Host $dir

          $inf = Join-Path $dir "viogpudo.inf"
          $cat = Join-Path $dir "viogpudo.cat"

          if (-not (Test-Path $inf)) {
            throw "Matching viogpudo.inf not found in $dir"
          }

          if (-not (Test-Path $cat)) {
            throw "Matching viogpudo.cat not found in $dir"
          }

          New-Item -ItemType Directory -Force artifact

          Copy-Item $sys.FullName artifact\viogpudo.sys
          Copy-Item $inf artifact\viogpudo.inf
          Copy-Item $cat artifact\viogpudo.cat

          Get-ChildItem artifact

      - name: Verify INF
        shell: powershell
        run: |
          Get-Content artifact\viogpudo.inf

          if (-not (Select-String `
              -Path artifact\viogpudo.inf `
              -Pattern 'PCI\\VEN_8086&DEV_4682')) {
            throw "8086:4682 missing from INF"
          }

      - name: Upload driver
        uses: actions/upload-artifact@v4
        with:
          name: Intel-UHD730-VirtIO-Win10-x64-fixed
          path: artifact
